#pragma once
#include <iostream>
#include <fstream>
#include <cstdint>
#include <vector>
#include <string>
#include <sstream>

uint32_t calculate_crc32 (std::istream &hashfile);
uint32_t calcutale_sum32 (std::istream &hashfile);


